import models from "../models/index.js"

export const generateAndSaveCertificate = async ({ volunteerId, eventId }) => {
    const { Event, Attendance, Certificate_Template, Certificate  } = models
}